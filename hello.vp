
$define!(sym symbol("some js symbol"))
$define!(val hash*(
  ("a" 1)
  ("b" 2)
  (sym hash*(
    ("c" 3)
    ("d" 4)))))

$define!(accessor at-key(sym))

$define!(test $fn((x y)
  $define!(z, 3)
  sum*(x y z 4)))

$define!(truth $fn(() #t))

show("help help!")
show(truth())

show(lens-get(accessor val))